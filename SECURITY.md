# Security

This package is pure standard-library Python with no runtime dependencies
and no network or file IO at runtime. If you believe you have found a
security issue anyway, report it privately via GitHub's "Report a
vulnerability" button on this repository rather than a public issue.
